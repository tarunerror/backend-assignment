# backend-assignment
Dockerized Django REST backend for a Credit Approval System with PostgreSQL, Celery, and Redis, featuring Excel data ingestion, credit scoring, compound-interest EMI calculation, and loan lifecycle APIs (`/register`, `/check-eligibility`, `/create-loan`, `/view-loan`, `/view-loans`).
